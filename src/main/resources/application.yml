spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${URL_DB}
    username: ${USERNAME_DB}
    password: ${PASSWORD_DB}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

#jdbc:postgresql://localhost:5432/crud_db
#root
#Godalzinh0